# GitHub Actions Runner Configuration
# Copy this file to .env and fill in your values

# GitHub Personal Access Token (NOT registration token!)
# This is used to generate registration tokens via API
# Required permissions: repo, admin:org (if using organization)
# Generate at: https://github.com/settings/tokens (classic token)
GITHUB_TOKEN=

# OR use the registration token directly (expires in 1 hour)
# Get it from: https://github.com/organizations/YOUR_ORG/settings/actions/runners/new
# or https://github.com/YOUR_USER/YOUR_REPO/settings/actions/runners/new
GITHUB_REGISTRATION_TOKEN=

# GitHub Organization (for org-level runner)
# Use this if you want the runner to be available for all repos in your organization
# Leave empty if using GITHUB_REPOSITORY instead
GITHUB_OWNER=

# GitHub Repository (for repo-level runner)
# Format: owner/repository-name
# Use this if you want the runner for a specific repository only
# Leave empty if using GITHUB_OWNER instead
GITHUB_REPOSITORY=

# Runner Name
# The name that will appear in GitHub's runner list
RUNNER_NAME=baremetal-ci-runner

# Runner Labels
# Comma-separated list of labels for workflow targeting
# Example: baremetal,jlink,mcu,stm32
RUNNER_LABELS=baremetal,jlink,mcu

# Runner Group
# The runner group to assign this runner to (organization runners only)
RUNNER_GROUP=default

# Runner Working Directory
# The directory where job workspaces will be created
RUNNER_WORKDIR=_work

# J-Link Device Serial Numbers (optional)
# Comma-separated list of J-Link serial numbers to pass through to container
# Leave empty to pass all J-Link devices
# Example: JLINK_SERIAL_NUMBERS=123456789,987654321,555666777
JLINK_SERIAL_NUMBERS=
